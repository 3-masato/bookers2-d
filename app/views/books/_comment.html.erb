<div>
  <div>
    <% book.book_comments.each do |c| %>
      <p><%= c.comment %></p>
    <% end %>
  </div>
  <div>
    <%= form_with model: [book, book_comment] do |f| %>
      <%= f.text_area :comment %>
      <%= f.submit "Comment" %>
    <% end %>
  </div>
</div>
